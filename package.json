{
  "name": "41-E-Joy",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "predev": "pnpm --filter order-service run prisma:generate",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test": "turbo run test"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=10"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
