# Release Notes Draft v0.1.0

## Highlights

- Core order/payment flow completed (`createOrder`, `initiatePayment`, `confirmPaymentCallback`)
- Staff ticket workflow online (`OPEN -> ACCEPTED -> RESOLVED`)
- Admin dashboard and ops queries (`dependencyHealth`, `paymentMetrics`)
- CI pipeline hardened with Prisma generate step

## Risk Notes

- Telebirr production callback samples still required for final signature conformance.
- External production infra and secret provisioning pending.

## Rollback

- Use `scripts/deploy-rollback.sh <previous_tag>`
- Follow `docs/deploy-versioned-runbook.md`
